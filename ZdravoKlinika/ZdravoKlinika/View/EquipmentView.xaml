<Window x:Class="ZdravoKlinika.View.EquipmentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pregled opreme" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dataGridEquipment" ItemsSource="{Binding Equipment}" Margin="0,62,0,0" SelectionChanged="dataGridEquipment_SelectionChanged" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Top" AutoGenerateColumns="False" SelectedIndex="-1" Width="650" Height="300" HeadersVisibility="Column" CanUserAddRows = "False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sifra" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Kolicina" Binding="{Binding Amount}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

